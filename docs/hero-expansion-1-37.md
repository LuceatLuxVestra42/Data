# Hero expansion — IDs 1 and 37

This slice tests whether the Hero 6 pattern generalizes without creating a global Hero framework.

Added:

- Hero 1: 马修 / 매튜, CN Hero-owned troop HP modifier 5%
- Hero 37: 古巨拉 / 쥬그라, CN Hero-owned troop HP modifier 15%

Both use actual pinned ConfigDataHeroInfo records. Korean names remain presentation-only. Both reused assets remain explicitly SP_ARTWORK and do not claim to be normal portraits.

The aggregate generated consumer contains Heroes 1, 6, and 37 so the separate presentation repository can prove multi-Hero rendering from one deterministic handoff.

Non-scope:

- global Hero population migration
- normal portrait admission
- Hero 1/37 relation migration
- search/filter framework
- raw-source ingestion framework
